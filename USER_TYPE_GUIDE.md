# دليل أنواع المستخدمين - User Types Guide

## ✅ نعم، يتم إنشاء حساب للعميل والحرفي!

الكود يدعم إنشاء حسابين مختلفين:

### 1️⃣ حساب العميل (Client)
- **الاختيار**: زر "عميل" 👤
- **الحقول المطلوبة**:
  - الاسم الكامل
  - البريد الإلكتروني
  - رقم الهاتف
  - كلمة المرور
- **لا يحتاج**: التخصص
- **بعد التسجيل**: يتم التوجيه إلى `index.html`

### 2️⃣ حساب الحرفي (Craftsman)
- **الاختيار**: زر "حرفي" 🛠️
- **الحقول المطلوبة**:
  - الاسم الكامل
  - البريد الإلكتروني
  - رقم الهاتف
  - كلمة المرور
  - **التخصص** (إجباري للحرفي)
- **بعد التسجيل**: يتم التوجيه إلى `craftsman-dashboard.html`

## كيف يعمل الكود:

### في صفحة التسجيل (signup.html):
```html
<!-- اختيار نوع المستخدم -->
<button data-type="client">عميل</button>
<button data-type="craftsman">حرفي</button>

<!-- حقل مخفي يحفظ النوع -->
<input type="hidden" id="userType" value="client">

<!-- حقل التخصص (يظهر فقط للحرفي) -->
<div id="specialtyGroup" style="display: none;">
    <select id="specialty">...</select>
</div>
```

### في auth.js:
```javascript
// عند الضغط على زر "حرفي"
craftsmanBtn.addEventListener('click', () => {
    userTypeInput.value = 'craftsman';
    specialtyGroup.style.display = 'block'; // إظهار حقل التخصص
});

// عند إرسال النموذج
const userData = {
    fullName,
    email,
    phone,
    password,
    userType, // 'client' أو 'craftsman'
    specialty: userType === 'craftsman' ? specialty : undefined
};
```

### البيانات المرسلة إلى Xano:
```json
{
    "fullName": "أحمد محمد",
    "email": "ahmed@example.com",
    "phone": "01234567890",
    "password": "password123",
    "userType": "craftsman", // أو "client"
    "specialty": "carpentry" // فقط للحرفي
}
```

## التحقق من النوع:

### في Xano Query:
تأكد من أن Query في Xano:
1. يقبل حقل `userType` (client أو craftsman)
2. يقبل حقل `specialty` (اختياري - فقط للحرفي)
3. يحفظ النوع في قاعدة البيانات

### مثال على Query في Xano:
```javascript
// في Xano Query
const user = {
    name: input.fullName,
    email: input.email,
    phone: input.phone,
    password: hashPassword(input.password),
    user_type: input.userType, // 'client' أو 'craftsman'
    specialty: input.specialty || null // فقط للحرفي
};

// حفظ في قاعدة البيانات
db.users.create(user);
```

## التحقق من العمل:

### للتحقق من أن الكود يعمل:
1. افتح `signup.html`
2. اختر "عميل" - يجب أن يختفي حقل التخصص
3. اختر "حرفي" - يجب أن يظهر حقل التخصص
4. املأ النموذج
5. اضغط "إنشاء الحساب"
6. تحقق من console - يجب أن ترى:
   ```
   Signup form submitted
   Calling signup API...
   Signup response: { ... }
   ```

## ملاحظات مهمة:

1. **التخصص إجباري للحرفي**: إذا اختار المستخدم "حرفي" ولم يختر تخصص، سيظهر خطأ
2. **التخصص غير مطلوب للعميل**: لا يتم إرسال `specialty` إذا كان `userType === 'client'`
3. **التوجيه بعد التسجيل**: 
   - العميل → `index.html`
   - الحرفي → `craftsman-dashboard.html`

## إذا لم يعمل:

1. تحقق من أن Xano Query يقبل `userType` و `specialty`
2. تحقق من console للأخطاء
3. تأكد من أن Query ID صحيح في `api.js`

---

**الخلاصة**: ✅ نعم، الكود يدعم إنشاء حساب للعميل والحرفي بشكل كامل!

