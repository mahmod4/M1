# دليل الأمان - Security Guide

## الحماية المُنفذة

### 1️⃣ حماية الصفحات (Page Protection)
- ✅ **auth-guard.js**: نظام حماية تلقائي لجميع الصفحات المحمية
- ✅ التحقق من تسجيل الدخول قبل الوصول للصفحات
- ✅ إعادة التوجيه التلقائي لصفحة تسجيل الدخول
- ✅ حفظ URL للعودة بعد تسجيل الدخول

**الصفحات المحمية:**
- `profile.html`
- `my-orders.html`
- `chat.html`
- `create-order.html`
- `add-review.html`
- `admin-dashboard.html`

### 2️⃣ حماية API (API Security)
- ✅ تنظيف Endpoints من أي محاولات injection
- ✅ تنظيف البيانات قبل الإرسال (XSS Protection)
- ✅ معالجة أخطاء 401 (غير مصرح) و 403 (ممنوع)
- ✅ حذف التوكن تلقائياً عند انتهاء الصلاحية
- ✅ عدم عرض تفاصيل الأخطاء الحساسة في الإنتاج

### 3️⃣ حماية من XSS (Cross-Site Scripting)
- ✅ تنظيف جميع المدخلات قبل الإرسال
- ✅ إزالة أي محاولات `<script>` injection
- ✅ إزالة `javascript:` protocols
- ✅ إزالة event handlers مثل `onclick=`

### 4️⃣ إدارة التوكن (Token Management)
- ✅ حفظ التوكن في localStorage بشكل آمن
- ✅ التحقق من صحة التوكن مع السيرفر
- ✅ التحقق من انتهاء صلاحية JWT tokens
- ✅ حذف التوكن عند تسجيل الخروج
- ✅ حذف التوكن عند انتهاء الصلاحية

### 5️⃣ حماية البيانات الحساسة
- ✅ عدم عرض تفاصيل الأخطاء في console في الإنتاج
- ✅ تنظيف البيانات قبل عرضها في الصفحة
- ✅ حماية من عرض معلومات حساسة

## كيفية العمل

### auth-guard.js
يتم تحميله تلقائياً في جميع الصفحات المحمية ويقوم بـ:
1. التحقق من وجود التوكن
2. التحقق من صحة التوكن مع السيرفر
3. إعادة التوجيه لصفحة تسجيل الدخول إذا لزم الأمر

### api.js
يحتوي على:
1. `sanitizeRequestBody()`: تنظيف البيانات من XSS
2. معالجة أخطاء 401/403
3. تنظيف Endpoints

## التوصيات الإضافية

### للإنتاج (Production):

1. **HTTPS**: استخدم HTTPS دائماً
2. **CSP Headers**: أضف Content Security Policy headers
3. **CORS**: تأكد من إعداد CORS بشكل صحيح في الباك إند
4. **Rate Limiting**: أضف rate limiting في الباك إند
5. **Input Validation**: تحقق من جميع المدخلات في الباك إند أيضاً
6. **SQL Injection**: تأكد من استخدام parameterized queries في الباك إند
7. **Session Management**: استخدم secure, httpOnly cookies للتوكن في الإنتاج

### مثال على CSP Header:
```html
<meta http-equiv="Content-Security-Policy" content="default-src 'self'; script-src 'self' 'unsafe-inline' https://accounts.google.com; style-src 'self' 'unsafe-inline' https://fonts.googleapis.com;">
```

## اختبار الأمان

### اختبار الحماية:
1. حاول الوصول لصفحة محمية بدون تسجيل دخول
2. حاول إرسال `<script>alert('XSS')</script>` في أي حقل
3. حاول الوصول لـ API بدون توكن
4. تحقق من حذف التوكن عند انتهاء الصلاحية

## ملاحظات مهمة

⚠️ **localStorage**: في الإنتاج، يُفضل استخدام httpOnly cookies بدلاً من localStorage للتوكن

⚠️ **CORS**: تأكد من إعداد CORS في Xano للسماح فقط للنطاقات المصرح بها

⚠️ **API Keys**: لا تضع API keys في الكود المصدري، استخدم environment variables

---

تم التطوير مع مراعاة أفضل ممارسات الأمان 🔒

